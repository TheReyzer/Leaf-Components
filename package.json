{
    "scripts": {
        "dev": "eleventy --serve & tailwind -i ./src/tailwind.css -o ./_site/css/style.css --watch",
        "build": "eleventy & tailwind build -i ./src/tailwind.css -o ./_site/css/style.css --minify"
    },
    "devDependencies": {
        "@11ty/eleventy": "^2.0.0",
        "tailwindcss": "^3.2.4"
    }
}
